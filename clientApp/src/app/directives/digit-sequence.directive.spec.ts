import { DigitSequenceDirective } from './digit-sequence.directive';

describe('DigitSequenceDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitSequenceDirective();
    expect(directive).toBeTruthy();
  });
});
